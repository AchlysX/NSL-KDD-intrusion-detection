# NSL-KDD-intrusion-detection
A machine learning project for intrusion detection using the NSL-KDD dataset. Includes data preprocessing, feature encoding, SMOTE balancing, model training, and evaluation of classifiers such as Decision Tree, Random Forest, and SVM to detect normal vs malicious network traffic.
